stages:
  - module-pipelines

frontend:
  stage: module-pipelines
  trigger:
    include:
     - local: 'front.yml'
     - template: Security/SAST.gitlab-ci.yml
  only:
    changes:
     - frontend/**/*     